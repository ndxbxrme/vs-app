@import '../../variables'
.invited-page
  form
    display flex
    flex-direction column
    max-width 320px
    input[type='text'], input[type='email'], input[type='password']
      vs-input()
    input[type='submit']
      vs-button()
    input
      margin-bottom 2rem